plugin: aws_ec2
regions:
  - ap-southeast-2
hostnames: private-dns-name
keyed_groups:
  - key: tags['Environment']
    prefix: env
  - key: tags['Role']
    prefix: role
#compose:
#  ansible_host: public-dns-name  # NEW: Explicitly set ansible_host variable