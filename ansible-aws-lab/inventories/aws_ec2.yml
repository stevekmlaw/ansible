plugin: aws_ec2
regions:
  - ap-southeast-2
hostnames: tag:Name
keyed_groups:
  - key: tags['Environment']
    prefix: env
  - key: tags['Role']
    prefix: role
compose:
  ansible_host: private_dns_name # NEW: Explicitly set ansible_host variable